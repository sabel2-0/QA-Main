# QA-Main

Standalone QA MVC app.

## Run locally

1. Install .NET 10 SDK.
2. From this repo root, run `dotnet run`.
3. Open `http://127.0.0.1:5000` or the URL shown in the terminal.

Default route goes to `QA/Dashboard`.
